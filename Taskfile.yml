version: '3'

# This project is archived.
# While this design was interesting, the author (Andy) is going to
# have another go at this that's much slimmer.

tasks:
  build:
    desc: Compiles the minimal placeholder binary.
    cmds:
      - go build -o dux main.go
