# This workflow is disabled — the project is archived.
# While this design was interesting, the author (Andy) is going to
# have another go at this that's much slimmer.

name: Validate (archived)

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Archived
        run: |
          echo "This project is archived."
          echo "While this design was interesting, the author (Andy) is going to"
          echo "have another go at this that's much slimmer."
